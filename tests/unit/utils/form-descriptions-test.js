import formDescriptions from 'ember-quickstart/utils/form-descriptions';
import { module, test } from 'qunit';

module('Unit | Utility | form descriptions');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formDescriptions();
  assert.ok(result);
});
