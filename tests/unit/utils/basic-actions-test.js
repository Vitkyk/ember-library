import basicActions from 'ember-quickstart/utils/basic-actions';
import { module, test } from 'qunit';

module('Unit | Utility | basic actions');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = basicActions();
  assert.ok(result);
});
