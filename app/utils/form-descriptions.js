export function createFormDescriptionElement(title, index, componentType, type, placeholder) {
  return { title, index, componentType, type, placeholder };
}

export default function formDescriptions() {
  return true;
}
